<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.sdpteam15.polyevents">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PolyEvents">
        <activity android:name=".admin.ZoneManagementListActivity"></activity>
        <activity
            android:name=".admin.EventManagementActivity"
            android:label="@string/title_activity_event_management"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".admin.UserManagementActivity"
            android:label="@string/title_activity_user_management"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".admin.ZoneManagementActivity"
            android:label="@string/title_activity_zone_management"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".admin.ItemRequestManagementActivity"
            android:label="@string/title_activity_item_request_management"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".ItemsAdminActivity" />
        <activity
            android:name=".ItemRequestActivity"
            android:label="@string/title_activity_item_request"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".EventActivity"
            android:parentActivityName=".MainActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
             android:value="${MAPS_API_KEY}"
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_api_key" />

        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"> <!-- the main activity is not recreated when we press the back button. Src: https://stackoverflow.com/questions/16434445/using-actionbar-home-as-up-button-home-activity-saveinstancestate-is-always-nul -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>